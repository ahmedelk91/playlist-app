<h1>Kool Jamz</h1>

<h2>Playlists <a href="/playlists/new">(+)</a></h2>

<ul>
  <% @playlists.each do |playlist| %>
  <li>
    <a href="/playlists/<%= playlist.id %>">
      <%= playlist.title %>
    </a>
  </li>
  <% end %>
</ul>
